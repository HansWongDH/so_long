initialize map
get map info with get next line
store them in char **str;

if (length is different)
if first and end isn't full of wall
if player is absent
if exit is absent
if collectable is absent
return 0

build the map
map movement key to minilibx function
